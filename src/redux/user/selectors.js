export const getUserAuthStatus = (state) => state.user.authStatus;
export const getUserAuthError = (state) => state.user.authError;
export const getUserName = (state) => state.user.name;
export const getUserAvatar = (state) => state.user.avatar;
